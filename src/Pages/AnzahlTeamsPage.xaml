<Page x:Class="Hero.Pages.AnzahlTeamsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:controls="clr-namespace:Hero.Controls"
      mc:Ignorable="d" 
      MinHeight="720" MinWidth="1280"
      Title="AnzahlTeamsPage">
    <Grid Background="{StaticResource BackgroundColorBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:NumberSelector x:Name="AnzahlTeamsNumberSelector" Grid.Row="0" VerticalAlignment="Bottom" 
                                 HorizontalAlignment="Center" Minimum="1" Maximum="8"/>
    </Grid>
</Page>
